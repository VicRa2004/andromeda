// Import Button from ui-react source package (relative import for development)
// The component uses BEM classes and CSS custom properties for theming
import {
  Button,
  type ButtonProps,
} from "../../../packages/ui-react/src/Button";

export { Button };
export type { ButtonProps };
